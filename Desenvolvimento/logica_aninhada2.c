#include <stdio.h>

    int main(){

        int idade, dependentes;
        float renda;
    
    // A primeira condição idade do usuário entre 18 e 65 anos.
    // A segunda condição se a renda do usuário é menor que 3000.
    // A terceira condição verificar se o número de dependentes é maior que 2.

    //Entrada dos dados do usuário:    
    printf("\n\nDigite a sua idade: \n");
    scanf("%d", &idade);
    printf("Digite a tua renda: \n");
    scanf("%f", &renda);
    printf("Digite o número de dependentes: \n");
    scanf("%d", &dependentes);

    //Estrutura de filtragem:
    if (idade < 18 || idade > 65) {}


    }